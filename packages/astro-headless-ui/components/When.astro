---
interface Props {
    [key: string]: any;
}

const values = Object.entries(Astro.props).filter(([k, _]) => !['slot'].includes(k))
---

{ !(!!values.length && !values.every(([_, v]) => !!v))
    ? <slot />
    : <slot name="false"/>
}