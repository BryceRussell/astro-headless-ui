---
interface Props {
  children: any;
  [key: string]: any;
}

const values = Object.values(Astro.props)
---

{ !(!!values.length && !values.every(v => !!v))
    ? <slot />
    : <slot name="false"/>
}