---
const props = Object.entries(Astro.props).filter(([k, _]) => !['slot'].includes(k))
---

{ Boolean(!!props.length && props.every(([_, v]) => Boolean(v)))
    ? <slot />
    : <slot name="false"/>
}